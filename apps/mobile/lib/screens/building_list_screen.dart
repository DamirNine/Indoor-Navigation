import 'package:flutter/material.dart';
class BuildingListScreen extends StatelessWidget {
  const BuildingListScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Buildings')));
}
